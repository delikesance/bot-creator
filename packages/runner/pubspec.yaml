name: bot_creator_runner
version: 1.5.13
description: CLI runner for bot_creator — loads a ZIP export and starts the bot.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.7.2

dependencies:
  bot_creator_shared:
    path: ../shared
  archive: ^4.0.9
  args: ^2.5.0
  http: ^1.6.0
  logging: ^1.3.0
  nyxx: ^6.8.1
  sqlite3: ^3.2.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.8
